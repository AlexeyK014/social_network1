import React from 'react';
import preloader from '../../../Loader/preloader.svg';

let Preloader = (props) => {
    return <div>
            <img src={preloader}/>
        </div>
}

export default Preloader;